hello = function() {
    return "Hello, World!";
}

// With arrow function
hello = () => {
    return "Hello, World!";
}

// if it has only one statement
hello = () => "Hello, World!";

// With parameters
hello = (val) => "Hello " + val;

// Without parenthesis
hello = val => "Hello " + val;
